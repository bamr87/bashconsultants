source "https://rubygems.org"

# Gemfile for Azure builds: omit jekyll-remote-theme so builds use the local gem theme

gem 'github-pages'

# Unconstrained to match the untagged remote_theme in _config.yml: the Pages and
# Azure stacks must resolve the SAME theme, and fleet policy is to track latest.
# `bundle update jekyll-theme-zer0` is what actually advances this stack.
gem 'jekyll-theme-zer0'

group :jekyll_plugins do
  # Keep other plugins, but omit jekyll-remote-theme for Azure
  gem 'jekyll-feed', "~> 0.17"
  gem 'jekyll-sitemap' , "~> 1.4.0"
  gem 'jekyll-seo-tag', "~> 2.8.0"
  gem 'jekyll-paginate', '~> 1.1'
  # Theme layouts (v1.x) use {% include_cached %} — required, not optional
  gem 'jekyll-include-cache', '~> 0.2'
end

# Docker support
gem "ffi", "~> 1.17.0"
gem 'webrick', "~> 1.7"
gem 'commonmarker', '0.23.10'
